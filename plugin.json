{
  "name": "legion",
  "displayName": "LegionIO",
  "version": "2.1.7",
  "description": "Routes LLM inference through the LegionIO daemon and fails closed when the daemon is unavailable. Syncs the daemon's live model catalog into Kai and exposes daemon tools for knowledge, memory, workers, and tasks.",
  "icon": {
    "lucide": "cpu"
  },
  "author": "John Hollow <https://github.com/iamhollow>",
  "permissions": [
    "config:read",
    "config:write",
    "tools:register",
    "ui:banner",
    "ui:settings",
    "network:fetch",
    "state:publish",
    "agent:inference-provider",
    "agent:register-runtime",
    "agent:register-cli-tool"
  ],
  "configSchema": {
    "type": "object",
    "properties": {
      "enabled": {
        "type": "boolean",
        "default": true
      },
      "daemonUrl": {
        "type": "string",
        "default": "http://127.0.0.1:4567"
      }
    }
  }
}
